<a href="{{ path('conversation_index') }}" class="relative inline-flex items-center gap-2 px-3 py-1.5 rounded-lg hover:bg-white/10 transition">
  <span class="font-medium">Messages</span>
  {% if count > 0 %}
    <span class="inline-flex items-center justify-center min-w-5 h-5 px-1 text-[11px] rounded-full bg-blue-600 text-white font-bold">{{ count }}</span>
  {% endif %}
</a>

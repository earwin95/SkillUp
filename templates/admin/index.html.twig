{% extends 'base.html.twig' %}

{% block title %}Espace Admin{% endblock %}

{% block body %}
    <div style="max-width: 600px; margin: 50px auto; text-align: center; padding: 30px; border: 1px solid #ccc; border-radius: 10px;">
        <h1>Bienvenue dans l’espace admin</h1>

        <p><strong>Identifiant :</strong> {{ app.user.email }}</p>
        <p><strong>Nom d'utilisateur :</strong> {{ app.user.username }}</p>
        <p><strong>Rôles :</strong> {{ app.user.roles|join(', ') }}</p>

        <a href="{{ path('app_logout') }}" 
           style="display: inline-block; margin-top: 20px; padding: 10px 20px; background-color: #dc3545; color: white; text-decoration: none; border-radius: 5px;">
           🚪 Se déconnecter
        </a>
    </div>
{% endblock %}

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>{% block title %}SkillUp{% endblock %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root{
      --bg: #eaf1f8; --ink:#0f172a; --muted:#6b7280;
      --glass:#ffffffa8; --stroke: rgba(255,255,255,.45);
      --primary:#2563eb; --primary-2:#06b6d4;
      --nav1:#0f172a; --nav2:#1f2937;
      --radius: 24px; --shadow: 0 20px 50px rgba(15,23,42,.18);
    }
    *{box-sizing:border-box} html,body{height:100%}
    body{
      margin:0; font:15px/1.6 ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto;
      color:var(--ink);
      background:
        linear-gradient(180deg, rgba(255,255,255,0) 0%, rgba(148,163,184,.25) 100%),
        url('https://images.unsplash.com/photo-1477959858617-67f85cf4f1df?q=80&w=1600&auto=format&fit=crop') center/cover fixed,
        var(--bg);
      padding: 24px;
    }

    /* App card */
    .app{
      width:min(1200px,100%); margin:0 auto; overflow:hidden;
      background: linear-gradient(180deg, rgba(255,255,255,.82), rgba(255,255,255,.68));
      border:1px solid var(--stroke); backdrop-filter: blur(12px);
      border-radius: 26px; box-shadow: var(--shadow);
    }

    /* NAVBAR */
    .nav{
      display:flex; align-items:center; justify-content:space-between;
      padding: 12px 18px;
      background: linear-gradient(90deg, var(--nav1), var(--nav2));
      color:#e5edff;
    }
    .brand{display:flex; align-items:center; gap:.8rem}
    .brand svg{display:block}
    .brand-title{font-weight:800; letter-spacing:.2px; font-size:1.05rem;}
    .nav-links{display:flex; gap:18px; flex-wrap:wrap;}
    .nav a{
      color:#dbeafe; text-decoration:none; font-weight:600; opacity:.9;
      position:relative; padding:.25rem .4rem; transition:opacity .2s, color .2s;
    }
    .nav a::after{
      content:''; position:absolute; left:0; bottom:-4px; width:100%; height:2px;
      background: linear-gradient(90deg, var(--primary), var(--primary-2));
      transform:scaleX(0); transform-origin:right; transition:transform .25s ease;
    }
    .nav a:hover{opacity:1; color:#fff}
    .nav a:hover::after{transform:scaleX(1); transform-origin:left}
    .pill{
      background:linear-gradient(90deg, #111827, #0b1324);
      color:#e5edff; border:1px solid rgba(255,255,255,.12);
      padding:.5rem .85rem; border-radius:999px; text-decoration:none; font-weight:700;
      transition:transform .15s, box-shadow .15s;
    }
    .pill:hover{transform:translateY(-1px); box-shadow:0 8px 18px rgba(2,6,23,.25)}

    /* Page content wrapper (petit top margin compact) */
    .page { padding: 18px 42px 32px; }
    @media (max-width: 900px){ .page{padding:16px 18px 28px} }
  </style>
</head>
<body>
  <div class="app">
    <!-- NAVBAR -->
    <div class="nav">
      <div class="brand">
        {# LOGO SkillUp #}
        <svg width="36" height="36" viewBox="0 0 64 64" aria-label="SkillUp" role="img">
          <defs>
            <linearGradient id="g1" x1="0" y1="0" x2="1" y2="1">
              <stop offset="0%" stop-color="#2563eb"/><stop offset="100%" stop-color="#06b6d4"/>
            </linearGradient>
          </defs>
          <path d="M20 5h24a7 7 0 0 1 6.06 3.5l12 20.8a7 7 0 0 1 0 6.4l-12 20.8A7 7 0 0 1 44 60H20a7 7 0 0 1-6.06-3.5L1.94 35.7a7 7 0 0 1 0-6.4L13.94 8.5A7 7 0 0 1 20 5Z" fill="url(#g1)"/>
          <path d="M19 33c0-6 6-9 13-9h3v5h-3c-4 0-7 1.6-7 4 0 2.2 2 3.7 6.4 4.4 7.2 1.2 11.6 4 11.6 9.6S38.7 54 31 54c-6.7 0-12-3-12.6-9.4h6.3c.5 3 3 4.6 6.3 4.6 3.6 0 6-1.5 6-4 0-2.4-2.1-3.6-6.6-4.4C22 39.6 19 36.7 19 33Z" fill="white" opacity=".95"/>
        </svg>
        <div class="brand-title">SkillUp</div>
      </div>

      <div class="nav-links">
        <a href="{{ path('app_home') }}">Accueil</a>
        <a href="{{ path('offer_index') }}">Offres</a>
        {% if app.user %}
          <a href="{{ path('app_exchange_requests') }}">Mes demandes</a>
          <a href="{{ path('app_profile') }}">Profil</a>
          {% if 'ROLE_ADMIN' in app.user.roles %}<a href="{{ path('app_admin') }}">Admin</a>{% endif %}
          <a class="pill" href="{{ path('app_logout') }}">Déconnexion</a>
        {% else %}
          <a href="{{ path('app_login') }}">Connexion</a>
          <a class="pill" href="{{ path('app_register') }}">Créer un compte</a>
        {% endif %}
      </div>
    </div>

    <div class="page">
      {% block body %}{% endblock %}
    </div>
  </div>
</body>
</html>
